server:
  port: 8080 # 端口浩
  servlet:
    context-path: / # 上下文路径
# 连接数据库的四大金刚
spring:
  datasource:
    username: cancan
    password: 123456
    url: jdbc:mysql://192.168.240.200:3306/edu?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
# mybatis配置
mybatis:
  type-aliases-package: edu.scu.edu.domain
  mapper-locations: classpath:mapper/*Mapper.xml
